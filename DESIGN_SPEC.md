# 위치 알람(Location Alarm) 화면 정의서 (Figma Design Spec)

> 본 문서는 [FEATURE.MD](FEATURE.MD) · [REQUIREMENTS_SPEC.md](REQUIREMENTS_SPEC.md)의 기능 요구사항을
> **실제 Figma 디자인과 1:1로 대조**하여 정리한 화면 정의서입니다.
> 각 화면별로 **목적 / 진입 경로 / 주요 컴포넌트 / 상태 / 연결된 요구사항**을 명시했습니다.
>
> - **Figma 위치**: 페이지 `위치알람` (채널 `t0djpnvs`)
> - **대상 디바이스**: 모바일 (375 × 812 기준, Android 12+)
> - **디자인 방향**: Clean Friendly (Light) · Material Icons
> - **작성 기준**: FEATURE.MD + REQUIREMENTS_SPEC.md + 구현된 Figma 프레임

---

## 0. 디자인 시스템 (Design Tokens)

### 컬러
| 토큰 | 값 | 용도 |
|------|-----|------|
| `primary` | `#4F6CF7` (인디고) | 브랜드, 주요 액션, 활성 상태 |
| `primary-tint` | `#EEF1FE` | 아이콘 배경, 선택 영역 |
| `success` | `#22C55E` / 텍스트 `#1FA463` | ON 상태, 진입(enter) |
| `warning` | `#F59E0B` / 텍스트 `#C77700` | 이탈(exit), 권한 경고, 알람(AlarmManager) |
| `danger` | `#EF4444` | 삭제, 로그인 실패, 자동 OFF |
| `text` | `#1A1D23` | 본문 제목 |
| `text-sub` | `#6B7280` | 보조 텍스트 |
| `border` | `#E5E8EC` | 카드/입력 테두리 |
| `bg` | `#F4F6F8` | 화면 배경 |
| `surface` | `#FFFFFF` | 카드/시트 |
| `scrim` | `#1F2637` (불투명 표현) | 다이얼로그 딤 배경 |

### 타이포그래피
- 폰트: **Noto Sans KR** (한글) / Inter (라틴·숫자) — 한글 폴백 적용
- 단계: 헤드라인 30 / 타이틀 18–22 / 바디 13–16 / 캡션 11–12

### 모양 · 깊이
- Radius: 카드 18 · 다이얼로그 22 · 버튼 14 · 입력/세그먼트 12 · 칩 8
- Shadow: 카드 `y6 blur16 a0.06` · Primary 버튼 `y8 blur16 a0.32` · 다이얼로그 `y12 blur32 a0.18`

### 아이콘
- **Material Icons** 벡터 사용(이모지 미사용)
- 매핑: 위치 `location_on` · 알림/푸시 `notifications` · **알람 `alarm`** · 티켓 `confirmation_number` · 검색 `search` · 추가 `add` · 뒤로 `arrow_back` · 광고 `movie` · 경고 `warning` · 시계 `access_time` · 결제대기 `hourglass_empty` · 실패 `close` · 삭제 `delete` · 체크 `check`

---

## 화면 목록 & 플로우 맵

| # | Figma 프레임 | 역할 | 연결 요구사항 |
|---|--------------|------|----------------|
| 01 | `01_로그인` | 소셜 로그인 | SPEC §1 |
| 02 | `02_권한 온보딩` | 위치·알림 권한 사전 안내 | SPEC §2 |
| 03 | `03_알람 목록(메인)` | 루틴 조회 / ON·OFF / 진입점 | SPEC §3, §6 |
| 04 | `04_알람 생성/수정` (1/2) | 위치 조건 (지도·반경·방식) | SPEC §4-1 |
| 04-2 | `04-2_요일·시간·알림 설정` (2/2) | 시간 조건 + 알림 수단 | SPEC §4-2, §4-3 |
| 04-3 | `04-3_시간 선택 (Time Picker)` | 시작/종료 시간 휠 피커 | SPEC §4-2 |
| 04-4 | `04-4_알람 상세` | 루틴 상세 + 수정/삭제 진입 | SPEC §3 |
| 04-5 | `04-5_삭제 확인` | 삭제 확인 다이얼로그 | SPEC §3 |
| 05 | `05_티켓 충전` | 광고 보상 + 인앱 결제 | SPEC §7 |
| 06 | `06_알람 발생(풀스크린)` | 알람 트리거 표시 | SPEC §5 |
| D1~D8 | 예외/안내 다이얼로그 | 티켓·권한·결제·검증 예외 | SPEC §5~7, 시나리오 E-1~E-10 |

### 메인 플로우
```
01 로그인
  └─ 성공 → 02 권한 온보딩 ──(허용)──→ 03 알람 목록(메인)
                                              │
        ┌─────────────────────────────────────┼───────────────────────────┐
        │ (+) 버튼                              │ 카드 토글 ON               │ 카드 탭
        ▼                                      ▼                            ▼
   04 위치 설정(1/2)                      D1 티켓 차감 안내 ──확인──→ ON     04-4 알람 상세
        │ [다음]                          (티켓 0 → D2 티켓 부족 → 05)        │ [수정]→04 / [삭제]→04-5
        ▼                                                                    ▼
   04-2 시간·알림(2/2)                                                  04-5 삭제 확인 ──삭제──→ 목록
        │ 시간필드 탭 → 04-3 Time Picker → [확인]
        │ [저장하기]
        ▼
   03 알람 목록 (신규 항목 추가)

[백그라운드] 조건 충족 → 06 알람 발생(풀스크린) + 티켓 1 차감
                                  └ 티켓 0 도달 → D4 자동 OFF
하단 탭 "충전" → 05 티켓 충전 (광고 한도 → D5 / 결제 검증 → D6)
```

---

## 1. 로그인 — `01_로그인`

- **목적**: 소셜 로그인으로 빠른 진입, 계정 식별 (SPEC §1)
- **진입 경로**: 앱 최초 실행 / 로그아웃 상태
- **주요 컴포넌트**
  - 브랜드 영역: 앱 아이콘(`location_on`, 인디고 라운드 96) + "위치알람" + 태그라인
  - **카카오로 시작하기** 버튼(`#FEE500`)
  - **Google로 시작하기** 버튼(화이트 + 테두리)
  - 약관 동의 안내 텍스트
- **상태/예외**: 로그인 취소·실패 → **D7 로그인 실패** (E-9)
- **요구사항 매핑**: FEATURE [로그인], SPEC §1 (카카오=Kakao SDK / 구글=Firebase Auth)

---

## 2. 권한 온보딩 — `02_권한 온보딩`

- **목적**: 위치·알림 권한을 거부감 없이 허용 유도 (프리퍼미션) (SPEC §2)
- **진입 경로**: 로그인 성공 직후(최초) / 권한 미충족 재진입
- **주요 컴포넌트**
  - 타이틀 "딱 두 가지 권한이면 준비 끝이에요"
  - **위치 카드**(`location_on`, `위치 · 항상 허용`, **필수** 배지) — 백그라운드 감지 설명
  - **알림 카드**(`notifications`, `알림`, **필수** 배지)
  - 안내 박스: "‘앱 사용 중 허용’ → ‘항상 허용’ 2단계 동의" 설명
  - **권한 허용하고 시작하기** / `나중에 할게요`
- **상태/예외**
  - 백그라운드 미허용(E-1) → 목록 카드에 **권한 부족 배지** + **D3 권한 부족** 다이얼로그
  - 알림 거부(E-2) / OS에서 나중에 끔(E-3) → 설정 딥링크 안내
- **요구사항 매핑**: FEATURE [필요한 디바이스 권한], SPEC §2

---

## 3. 알람 목록(메인) — `03_알람 목록(메인)`

- **목적**: 등록한 루틴(Routine) 조회·관리, 생성/상세 진입 (SPEC §3)
- **진입 경로**: 권한 온보딩 이후 메인 / 저장 후 복귀
- **주요 컴포넌트**
  - 헤더: "내 위치알람" + **티켓 잔량 배지**(`confirmation_number` + "3장")
  - **루틴 카드**(Routine 단위) — 3가지 상태 예시:
    | 카드 | 상태 | 표현 |
    |------|------|------|
    | 학교정문 | ON(정상) | 초록 토글 ON, 진입/150m/알람 칩, 요일·시간 요약 |
    | 시청역 | ON + 권한 부족 | 토글 ON + **권한 부족 경고 스트립**(`warning`, E-1) |
    | 헬스장 | OFF | 회색 토글, 본문 디밍, 이탈/100m/푸시 칩 |
  - 카드 칩: **진입**(초록)/**이탈**(앰버) · 반경(`150m`) · 방식(`alarm`/`notifications`)
  - **FAB (+)** → 생성 진입 (`add`)
  - 하단 탭: 목록 / 충전 / 마이 (`list`/`confirmation_number`/`person`)
- **상태/동작**
  - 토글 ON 시 → **D1 티켓 차감 안내** (SPEC §6)
  - 티켓 0 ON 시도(E-4) → **D2 티켓 부족** 바텀시트 → 충전 유도
  - 카드 탭 → **04-4 알람 상세**(별도 화면, 수정/삭제) — SPEC §3 확정사항
  - 정렬: **생성 시점 기준**
- **요구사항 매핑**: FEATURE [위치 알람 목록], SPEC §3·§6

---

## 4. 알람 생성/수정 — 2단계 플로우

> 위치 조건(필수)과 시간·알림 조건을 **2단계로 분리**하여 입력 부담을 낮춤. 상단 `1/2`·`2/2` 단계 표시.

### 4-1. 위치 설정 (1/2) — `04_알람 생성/수정`
- **목적**: "어디서" 조건 — 좌표·반경·방식 (SPEC §4-1, 전부 필수)
- **주요 컴포넌트**
  - **지도 영역(화면의 절반, ~406px)** + 중심 핀(`location_on`) + 반경 원 오버레이
  - 지도 위 **검색바**(`search`, "장소·주소 검색") — 검색 선택 방식
  - **반경 슬라이더**: 50m~500m, 50m 단위 10단계, 현재값 표시(150m), 기본 50m
  - **알람 방식 세그먼트**: `진입`(기본) / `이탈`
  - 하단 **[다음]** 버튼
- **요구사항 매핑**: SPEC §4-1, FEATURE [위치 알람 생성] 1번 조건

### 4-2. 시간 · 알림 설정 (2/2) — `04-2_요일·시간·알림 설정`
- **목적**: "언제 / 어떻게 알릴지" 조건 (SPEC §4-2, §4-3)
- **주요 컴포넌트**
  - **요일 선택**(일~토 원형 칩, 다중 선택) — *필수*. 월~금 활성 예시
  - **시간 범위 체크박스**(`선택`) — 체크 시 시작/종료 필드 노출
  - **시작 / 종료 시간 필드**(`access_time` 아이콘, 08:00 / 09:30) → 탭 시 **04-3 Time Picker**
  - 검증 안내: "시작과 종료가 같으면 알람이 울리지 않아요" (E-10)
  - **알림 방식 세그먼트**: `알람`(`alarm`) / `앱 푸시`(`notifications`), 기본 앱 푸시
  - 하단 **[저장하기]**
- **검증 규칙**: 요일 필수 / 시간 선택 / 시작 ≤ 종료 / 시작=종료 시 미동작(E-10)
- **요구사항 매핑**: SPEC §4-2·§4-3, FEATURE 2·3번 조건

### 4-3. 시간 선택 (Time Picker) — `04-3_시간 선택 (Time Picker)`
- **목적**: 시간 범위 체크박스 선택 시 노출되는 시간 입력 UI (SPEC §4-2)
- **주요 컴포넌트**: 바텀시트 **휠 피커**(오전·오후 / 시 / 분) + 중앙 선택 하이라이트 + `취소`/`확인`
- **요구사항 매핑**: USER_SCENARIOS 시나리오 B 9번 "시간 선택 체크박스 선택 시 DateTime Picker 노출"

### 4-4. 알람 상세 — `04-4_알람 상세`
- **목적**: 루틴 상세 확인 및 **수정/삭제 진입** (SPEC §3 — 별도 화면)
- **진입 경로**: 메인 목록 카드 탭
- **주요 컴포넌트**
  - 지도 프리뷰(위치 + 반경 원)
  - 정보 카드: 제목·주소·**ON 배지** + 키–값(알람 방식·반경·요일·시간·알림 방식)
  - **[수정하기]**(Primary) → 04 생성 플로우 재사용
  - **[삭제하기]**(`delete`, 빨강 아웃라인 — destructive 우선순위 낮춤) → **04-5**
- **요구사항 매핑**: SPEC §3 (수정/삭제 별도 화면)

### 4-5. 삭제 확인 — `04-5_삭제 확인`
- **목적**: 되돌릴 수 없는 삭제 전 최종 확인
- **주요 컴포넌트**: `delete`(빨강) 아이콘 + "이 알람을 삭제할까요?" + 경고 본문 + `취소` / **`삭제`(빨강)**
- **요구사항 매핑**: SPEC §3

---

## 5. 티켓 충전 — `05_티켓 충전`

- **목적**: 광고 보상·결제로 티켓 확보 (SPEC §7)
- **진입 경로**: 하단 탭 "충전" / 티켓 부족(D2) / 자동 OFF(D4)에서 유도
- **주요 컴포넌트**
  - **보유 티켓 히어로 카드**(인디고, "3장", `confirmation_number`)
  - **광고 보상 카드**(`movie`): "광고 보고 티켓 받기 · 시청 완료 시 +1장 · 오늘 1/3" + `받기`
  - **결제 상품 카드**(`confirmation_number`): "티켓 10장" + **₩2,000**
  - 정책 안내: 광고 하루 최대 3장·자정 초기화 / 결제 서버 검증 / 발생당 1장 차감
- **상태/예외**
  - 광고 한도 초과(E-6) → **D5 광고 한도** (버튼 비활성)
  - 광고 미완료(E-7) → 지급 없음 안내
  - 결제 검증 지연(E-8) → **D6 결제 확인 중**
- **요구사항 매핑**: FEATURE [티켓 추가 방법], SPEC §7

---

## 6. 알람 발생(풀스크린) — `06_알람 발생(풀스크린)`

- **목적**: 조건 충족 시 알람 트리거 표시(AlarmManager 강조) (SPEC §5)
- **진입 경로**: 백그라운드 지오펜스 진입/이탈 이벤트 + 요일·시간 충족
- **주요 컴포넌트**(다크 풀스크린)
  - 시간/날짜, 펄스 링 + 중앙 `location_on` 아이콘
  - `진입 알람` 칩, "학교정문 도착!", "설정한 150m 반경에 진입했어요"
  - **티켓 차감 표시**: `confirmation_number` + "티켓 1장 차감 · 남은 2장"
  - **[알람 끄기]** / `5분 뒤 다시 알림`
- **상태/동작**: 발생당 티켓 1 차감 / 쿨다운(시간 미설정=24h, 설정=범위 내 1회) / 티켓 0 → **D4 자동 OFF**
- **요구사항 매핑**: FEATURE [위치 알람 발생], SPEC §5·§6

---

## 예외 · 안내 다이얼로그 (D1~D8)

| ID | Figma 프레임 | 트리거 | 형태 | 액션 | 매핑 |
|----|--------------|--------|------|------|------|
| D1 | `D1_티켓 차감 안내` | 알람 ON 전환 | 센터 다이얼로그 | 취소 / **켜기** | SPEC §6 (안내만, 차감 없음) |
| D2 | `D2_티켓 부족(E-4)` | 티켓 0에서 ON 시도 | 바텀시트 | **충전하러 가기** | E-4 |
| D3 | `D3_권한 부족(E-1)` | 백그라운드 위치 미허용 | 센터 다이얼로그 | 나중에 / **설정으로 이동** | E-1 |
| D4 | `D4_자동 OFF(E-5)` | 발생으로 티켓 0 도달 | 센터 다이얼로그 | 닫기 / **충전하기** | E-5, SPEC §6 |
| D5 | `D5_광고 한도(E-6)` | 광고 보상 3회 초과 | 센터 다이얼로그 | 확인 | E-6 |
| D6 | `D6_결제 확인 중(E-8)` | 결제 서버 검증 대기 | 로딩 다이얼로그 | (대기) | E-8 |
| D7 | `D7_로그인 실패(E-9)` | 로그인 취소/인증 실패 | 센터 다이얼로그 | **다시 시도** | E-9 |
| D8 | `D8_시간 검증(E-10)` | 시작=종료 저장 시도 | 센터 다이얼로그 | 확인 | E-10 |

---

## 요구사항 ↔ 화면 커버리지 매트릭스

| 요구사항 (FEATURE / SPEC) | 반영 화면 | 상태 |
|----------------------------|-----------|------|
| 소셜 로그인(카카오/구글) | 01 | ✅ |
| 위치·알림 권한 안내 | 02 (+ D3) | ✅ |
| 위치 알람 목록 조회 | 03 | ✅ |
| 목록 ON/OFF 토글 | 03 (+ D1, D2) | ✅ |
| 아이템 정보·ON/OFF 노출 | 03 카드 | ✅ |
| 수정/삭제 (별도 화면) | 04-4, 04-5, 04(수정) | ✅ |
| 좌표 선택 (지도 이동/검색) | 04 (지도 + 검색바) | ✅ |
| 반경 50~500m·10단계·기본50 | 04 슬라이더 | ✅ |
| 알람 방식(진입/이탈·기본 진입) | 04 세그먼트 | ✅ |
| 요일 다중 선택(필수) | 04-2 | ✅ |
| 시작~종료 시간(선택·시작≤종료) | 04-2 + 04-3 | ✅ |
| 알림 방식(앱푸시/알람·기본 앱푸시) | 04-2 세그먼트 | ✅ |
| 위치 알람 발생 표시 | 06 | ✅ |
| 티켓 ON 조건·차감 안내 | D1, 03 | ✅ |
| 발생당 티켓 1 차감 | 06 | ✅ |
| 티켓 0 자동 OFF | D4 | ✅ |
| 광고 보상(+1, 하루 3개) | 05 (+ D5) | ✅ |
| 인앱 결제(10개/2,000원) | 05 (+ D6) | ✅ |

> **디자인 미표현(백엔드/시스템 영역, 화면 불필요)**: 지오펜스/포그라운드 서비스 동작, 자정 재등록(`MidnightReceiver`), 서버 토큰·영수증 검증, 쿨다운 로직 — 화면이 아닌 동작 규칙으로 06·05 안내 문구에 반영.

---

## 비고 (디자인 기준 메모)
- 모든 아이콘은 **Material Icons 벡터**로 통일(이모지 제거 완료).
- `진입`=초록·`이탈`=앰버, `알람`(AlarmManager)=`alarm` 아이콘·`앱 푸시`(NotificationManager)=`notifications` 아이콘으로 시스템 동작 차이를 시각적으로 구분.
- 생성 플로우는 명세상 단일 화면이었으나, 입력 항목이 많아 **위치(1/2) → 시간·알림(2/2)** 2단계로 분리(USER_SCENARIOS 수정안 반영).
- 본 문서의 화면 번호/이름은 Figma 프레임명과 동일하게 유지.

*작성 기준: FEATURE.MD · REQUIREMENTS_SPEC.md · USER_SCENARIOS.md + 구현된 Figma 디자인(페이지 `위치알람`)*
