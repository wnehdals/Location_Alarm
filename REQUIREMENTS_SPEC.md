# 위치 알람(Location Alarm) 기능 명세서

> 본 문서는 [FEATURE.MD](FEATURE.MD)에 수집된 요구사항을 기능 단위로 분류하고,
> 각 기능별로 **목적 / 대상 사용자 / 예상 동작**을 정리한 것입니다.
> 원문이 모호한 부분은 `📌 가정` 으로 명시했으며, 추후 확정이 필요한 항목은
> 문서 하단 [확정 필요 항목](#확정-필요-항목)에 모았습니다.

---

## 기능 분류 한눈에 보기

| # | 기능 영역 | 핵심 내용 |
|---|-----------|-----------|
| 1 | 인증/로그인 | 카카오·구글 소셜 로그인 |
| 2 | 권한 온보딩 | 위치·알림 권한 허용 유도 안내 |
| 3 | 위치 알람 목록 | 조회, ON/OFF, 수정/삭제 |
| 4 | 위치 알람 생성 | 좌표·반경·방식·요일·시간·알림수단 설정 |
| 5 | 위치 알람 발생 | 백그라운드 위치 수집 기반 알람 트리거 |
| 6 | 티켓(이용권) 시스템 | 알람 ON 및 발생 시 티켓 차감 |
| 7 | 티켓 충전 | 광고 시청 보상, 구글플레이 결제 |

---

## 1. 인증 / 소셜 로그인

**목적**
- 별도의 회원가입 절차 없이 빠르게 진입하도록 하여 가입 이탈을 최소화한다.
- 사용자별 위치 알람·티켓 데이터를 식별·동기화할 기준 계정을 확보한다.

**대상 사용자**
- 앱을 처음 실행하는 신규 사용자
- 기기 변경·재설치 후 기존 데이터를 이어가려는 기존 사용자

**예상 동작**
- 로그인 화면에서 **카카오 / 구글** 두 가지 소셜 로그인 방식을 제공한다.
- 소셜 인증 성공 시 사용자 식별자(계정)를 발급/연동하고 메인(위치 알람 목록)으로 진입한다.

**확정 사항**
- **인증 구현 수단**:
  - **카카오** — **Kakao SDK**(Kakao Login)로 인증.
  - **구글** — **Firebase Authentication**(Google 제공자)으로 인증.
- **백엔드 서버 존재**. 계정 식별·토큰 검증, 티켓 잔량, 결제 검증 등은 서버에서 관리한다.

**📌 가정**
- 이메일/비밀번호 등 자체 회원가입은 제공하지 않는다(소셜 로그인 전용).
- 로그인은 앱 사용을 위한 **필수** 단계로 가정한다(비로그인 둘러보기 미제공).
- 인증 토큰 처리:
  - 카카오: Kakao SDK 로그인으로 받은 토큰을 서버로 전달해 검증한다.
  - 구글: Firebase 인증으로 받은 ID 토큰을 서버에서 Firebase Admin으로 검증한다.
  - 검증 성공 후 서버가 발급한 세션/토큰으로 이후 API를 호출한다.
- 로컬(Room)은 캐시/오프라인 표시 용도로 사용하고, 티켓 등 민감 데이터의 정본은 서버로 둔다.

---

## 2. 디바이스 권한 온보딩

**목적**
- 위치 알람의 핵심 동작에 필요한 권한을, 사용자가 거부감 없이 허용하도록 사전 안내한다.
- 권한 거부로 인한 기능 미동작과 그로 인한 이탈/CS를 줄인다.

**대상 사용자**
- 최초 실행 사용자
- 권한을 거부했거나 OS 설정에서 권한을 끈 상태로 기능을 사용하려는 사용자

**예상 동작**
- 권한 요청 전, 권한이 **왜 필요한지**를 설명하는 안내 화면(프리퍼미션)을 노출한다.
- 안내 후 OS 권한 요청을 띄운다. 필요한 권한:
  - **위치 권한** — 백그라운드에서 지속 위치 수집이 필요하므로 "항상 허용" 수준 안내 포함
  - **알림 권한** — 앱 푸시/알람 노출용 (Android 13+ 런타임 알림 권한 대응)
- 권한 거부 시 기능 제약을 안내하고, OS 설정으로 이동하는 경로를 제공한다.

**📌 가정**
- 최소 SDK 31(Android 12) / 타깃 36 기준이므로,
  - 위치는 `ACCESS_FINE_LOCATION` + `ACCESS_BACKGROUND_LOCATION`,
  - 알림은 `POST_NOTIFICATIONS`(Android 13+) 가 대상이다.
- 백그라운드 위치는 2단계 동의(앱 사용 중 → 항상 허용) 흐름으로 안내한다.

---

## 3. 위치 알람 목록

**목적**
- 사용자가 등록한 위치 알람(루틴)을 한 화면에서 조회·관리한다.

**대상 사용자**
- 위치 알람을 1개 이상 등록한 사용자

**예상 동작**
- 저장된 위치 알람을 리스트로 조회한다.
- 각 아이템에는 다음이 노출된다:
  - 위치 알람 정보(장소/주소, 반경, 진입·이탈 방식, 요일, 시간 등 요약)
  - 현재 **ON/OFF 상태**
- 아이템의 토글로 위치 알람을 **ON/OFF** 전환한다.
  - ON 전환은 티켓 보유/차감 조건의 영향을 받는다([6. 티켓 시스템](#6-티켓이용권-시스템) 참조).
- 아이템 클릭 시 **수정 / 삭제**를 선택할 수 있다.

**확정 사항**
- "위치 알람 목록 아이템"은 **Routine 단위**
  (Routine = 여러 요일의 Alarm을 묶은 반복 알람 그룹).
- 아이템 클릭 후 수정/삭제는 **별도 화면**으로 진입해 처리한다.
- 목록 정렬 기준: **생성된 시점 기준 정렬**.

---

## 4. 위치 알람 생성

**목적**
- "어디서 / 언제 / 어떻게 알릴지"를 조합해 위치 기반 알람 조건을 등록한다.

**대상 사용자**
- 특정 장소 진입/이탈 시점에 알림을 받고 싶은 사용자
  (예: 출근길 특정 지점 통과, 귀가 시 알림, 특정 구역 이탈 알림 등)

**예상 동작**

### 4-1. 어떻게 실행할지 (위치 조건) — *전부 필수*
- **좌표 선택 방법** (둘 중 하나로 좌표 확정)
  - NAVER MAP에서 지도를 이동시켜 중심 좌표 선택
  - 장소/주소 검색으로 위치 선택
- **반경 선택**
  - 50M ~ 500M, 50M 단위, 총 10단계
  - 기본값: **50M**
- **알람 방식 선택**
  - 원형 범위 **안으로 진입**(enter)
  - 원형 범위 **밖으로 이탈**(exit)
  - 기본값: **진입(enter)**

### 4-2. 언제 실행할지 (시간 조건)
- **요일 다중 선택** (일·월·화·수·목·금·토) — *필수*
- **시작시간 ~ 종료시간** — *선택*
  - 시작시간 ≤ 종료시간 제약 (시작 > 종료 불가, 종료 < 시작 불가)

### 4-3. 알람 울리는 방식
- **앱 푸시 / 알람** 중 선택
- 기본값: **앱 푸시**
- **동작 차이(확정됨)**:
  - **앱 푸시** = `NotificationManager` 기반 일반 알림 노출
  - **알람** = `AlarmManager`를 사용한 알람 트리거(예약/정시성 보장, 알람 화면·소리 강조)

**제한사항(검증 규칙)**
- 4-1(위치 조건)의 세 항목(좌표 선택, 반경, 알람 방식)은 **모두 필수**.
- 4-2의 **요일은 필수**, 시작~종료 시간은 **선택**.
- 시간 미입력 시 해당 요일에 한해 **시간 제약 없이 동작**한다(요일 조건만 충족하면 발생).

**📌 가정**
- 시작/종료 시간은 **분 단위** 입력으로 가정.
- 시간 범위가 정상 입력되었고 시작=종료(범위 길이 0)인 경우는 **미동작**으로 처리한다.
  단, 시간 자체를 입력하지 않은 경우는 위 검증 규칙대로 **요일만으로 동작**한다.

---

## 5. 위치 알람 발생

**목적**
- 등록된 조건을 충족할 때 실제 알림(앱 푸시 또는 알람)을 발생시킨다.

**대상 사용자**
- ON 상태의 위치 알람을 보유한 사용자

**예상 동작**
- 백그라운드에서 위치 정보를 지속 수집한다.
- 다음 조건을 모두 만족하면 알람/앱 푸시를 발생시킨다:
  - 지정 **좌표 + 반경** 기준 **진입 또는 이탈** 이벤트 발생
  - 현재 시점이 설정한 **요일**에 해당
  - 설정한 **시작~종료 시간** 범위 내(시간 미설정 시 상시)
- 발생 시 사용자가 선택한 방식(앱 푸시/알람)으로 알린다.

**📌 가정**
- 위치 감지는 Google Play Services **Geofencing**(`GeofenceHelper` + `GeofenceBroadcastReceiver`)을 1차 수단으로 사용하고,
  `FusedLocationService`(포그라운드 서비스)를 보조 fallback으로 사용한다(코드베이스 기준).
- 자정에 `MidnightReceiver`로 당일 요일에 맞는 지오펜스를 재등록한다.
- **중복 발생 방지(쿨다운, 확정됨)**:
  - 시작~종료 **시간 범위를 설정하지 않은** 알람: 쿨다운 **하루(24시간)** — 같은 알람 하루 1회 발생.
  - 시작~종료 **시간 범위를 설정한** 알람: 해당 **시간 범위 내에서 이벤트 1회만** 발생
    (범위 내 첫 진입/이탈 1회 발생 후, 같은 날 같은 범위에서는 재발생 안 함).
- 1회 발생 시 티켓 1개가 차감된다([6. 티켓 시스템](#6-티켓이용권-시스템)).

---

## 6. 티켓(이용권) 시스템

**목적**
- 위치 알람 발생을 소비형 재화(티켓)와 연결해 수익화 및 사용량 제어 기반을 만든다.

**대상 사용자**
- 위치 알람을 ON 하거나 실제로 알람을 받는 모든 사용자

**예상 동작**
- **ON 조건**: 티켓이 **1개 이상** 있어야 위치 알람을 ON으로 전환할 수 있다.
- **ON 전환 시**: 티켓이 차감된다는 사실을 사용자에게 **안내**한다(안내만, 이 시점 차감 없음).
- **차감 규칙(확정됨)**: 위치 알람이 **1회 발생(울릴 때)할 때마다** 티켓 **1개 감소**.
  - ON 전환 시점에는 차감하지 않는다(**발생 차감만 적용**).
- **티켓 0 도달 시(확정됨)**: 해당 위치 알람을 **자동 OFF** 한다.
- 티켓이 부족하면 ON 전환을 막고 충전 경로([7. 티켓 충전](#7-티켓-충전))로 유도한다.

**확정 사항**
- 티켓은 **서버에서 사용자 계정 단위로 관리**되며 기기 변경 시에도 유지된다.

**📌 가정**
- 티켓 차감은 서버를 정본으로 처리하되, 발생 시점 즉시성을 위해 클라이언트 낙관적 반영 후 서버 동기화한다.
- 잔여 티켓이 여러 ON 알람에 공유되는 경우, 발생 순서대로 차감하고
  0이 되는 시점에 ON 상태인 알람을 자동 OFF 한다.

---

## 7. 티켓 충전

**목적**
- 사용자가 티켓을 확보하여 위치 알람을 계속 사용하도록 한다.

**대상 사용자**
- 티켓이 부족하거나 추가 확보를 원하는 사용자

**예상 동작**
- **광고 보상**: 구글 광고(리워드형)를 **시청 완료**하면 티켓 **1개** 지급.
  - **일일 한도(확정됨): 하루 최대 3개** (1일 광고 보상 3회 제한).
- **인앱 결제**: 구글플레이 결제로 티켓 구매 — **10개 / 2,000원**.

**📌 가정**
- 광고는 Google AdMob **리워드 광고**를 사용한다고 가정.
- 광고 일일 한도(3개)는 **사용자 계정 기준, 매일 자정 초기화**로 가정한다.
- 인앱 상품은 **소비성(consumable)** 상품으로 구성(구매 후 즉시 소비, 재구매 가능).
- **결제 검증(확정됨)**: 구글플레이 영수증을 **서버에서 검증** 후 티켓을 지급한다.
  (환불/취소 시 서버에서 티켓 회수 정책 필요 — 세부 정책 미정)
- **통화/국가(확정됨)**: **원(KRW) / 한국 스토어 단일 대응** (다국가/다통화 확장 없음).

---

## 확정 완료 항목

| 항목 | 확정 내용 |
|------|-----------|
| 티켓 차감 정책 | **발생(울릴 때) 차감만 적용** (ON 전환 차감 없음) |
| "알람" vs "앱 푸시" | 알람 = `AlarmManager` / 앱 푸시 = `NotificationManager` |
| 쿨다운(시간 미설정 시) | 동일 알람 **하루 1회**(쿨다운 24시간) |
| 시작=종료(범위 0) | **미동작**. 단, 요일은 필수이므로 선택 요일이면 동작 |
| 목록 정렬 | **생성 시점 기준 정렬** |
| 광고 보상 한도 | **하루 최대 3개** |
| 티켓 0 도달 | 해당 알람 **자동 OFF** |
| 목록 아이템 단위 | **Routine 단위** |
| 수정/삭제 UI | **별도 화면**으로 진입 |
| 결제 통화/국가 | **원(KRW) / 한국 단일** |
| 시간 범위 설정 알람 쿨다운 | **시간 범위 내 1회만** 발생 |
| 백엔드 | **서버 존재** — 계정·티켓·결제 검증을 서버에서 관리 |

## 확정 필요 항목 (남은 의사결정)

모든 핵심 의사결정이 확정되었습니다. 아래는 구현 단계에서 정책 확정이 필요한 **세부 항목**입니다.

- **결제 환불/취소 시 티켓 회수** 정책 세부 규칙.
- **티켓 차감 동기화** 방식(낙관적 반영 vs 서버 확정 후 반영)의 최종 채택.
- 서버 API 스펙(엔드포인트/스키마) 정의 — 구현 계획 단계에서 확정.

---

*작성 기준: FEATURE.MD 수집 요구사항 + 현재 코드베이스(`com.jdm.alarmlocation`) 아키텍처 참고*
